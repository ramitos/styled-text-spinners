```
const { Beam } = require('../');

<div>
  <Beam />
  <Beam helvetica />
  <Beam verdana />
  <Beam monospace />
  <Beam sansSerif />
  <Beam serif />
</div>
```

```
const { Beam } = require('../');
const Button = require('./Button').default;

<div>
  <Button>Loading <Beam /></Button>
  <Button>Loading <Beam helvetica /></Button>
  <Button>Loading <Beam verdana /></Button>
  <Button>Loading <Beam monospace /></Button>
  <Button>Loading <Beam sansSerif /></Button>
  <Button>Loading <Beam serif /></Button>
</div>
```
